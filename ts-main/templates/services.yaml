{{- range .Values.services}}
apiVersion: v1
kind: Service
metadata:
  name: {{.name}}
  labels:
    app: {{.name}}
    role: master
spec:
  ports:
    - port: 80
      targetPort: 80

  selector:
    app: {{.name}}
    role: master
---
{{- end}}